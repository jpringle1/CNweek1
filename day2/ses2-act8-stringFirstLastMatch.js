const work = "tex"

if (work.split(``)[0] == work.split(``).reverse()[0]) {
    console.log(`first letter of ${work} matches last`)
} else {
    console.log(`first letter of ${work} does not match last`)
}